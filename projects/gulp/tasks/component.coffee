#config = require('../config')
#gulp = require('gulp')
#component = require('html-component')
#
#gulp.task 'init', ()->
#  component.init()
#
#gulp.task 'create', ()->
#  name = config.args.name
#  if (name)
#    component.create(name)
#
